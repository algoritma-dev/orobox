# This file is yours: Orobox generates it once and never touches it again. Add your own stages,
# variables and jobs here.
#
# The Orobox jobs live in [[.IncludePath]], which `orobox ci-init` rewrites on every run.
include:
  - local: [[.IncludePath]]
