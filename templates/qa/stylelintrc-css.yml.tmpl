# Project-specific Stylelint configuration for plain CSS, generated once by `orobox qa-init`.
#
# Orobox layers this file on top of OroCommerce's own .stylelintrc-css.yml through `extends`, this
# file last, so the rules set here win. Do not copy Oro's configuration in.
rules: {}
