<?php

declare(strict_types=1);

use Rector\Config\RectorConfig;

/**
 * Project-specific Rector configuration, generated once by `orobox qa-init`.
 *
 * Orobox applies the shared standard first and this closure second, on the same RectorConfig, so
 * calls made here override the standard's. Returning anything but a closure or a
 * RectorConfigBuilder makes `orobox qa` fail with an explicit message rather than skip the file.
 */
return static function (RectorConfig $rectorConfig): void {
    // $rectorConfig->skip([__DIR__ . '/src/Migrations']);
};
