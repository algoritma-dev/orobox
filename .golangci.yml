version: "2"

run:
  timeout: 5m
  go: "1.21"

linters:
  default: none
  enable:
    - revive
    - misspell
    - ineffassign
  exclusions:
    presets: []

formatters:
  enable:
    - gofmt

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
