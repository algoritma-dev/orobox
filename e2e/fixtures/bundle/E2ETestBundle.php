<?php

declare(strict_types=1);

namespace Orobox\Bundle\E2ETestBundle;

use Symfony\Component\HttpKernel\Bundle\Bundle;

/**
 * The fixture bundle's bundle class.
 *
 * The class exists so the checkout has PHP the QA tools can walk: PHP-CS-Fixer's finder is
 * rooted at the bundle root for a bundle install, and a tool that walks an empty tree proves
 * nothing about being installed and configured.
 */
class E2ETestBundle extends Bundle
{
}
